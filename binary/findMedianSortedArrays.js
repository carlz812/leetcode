/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number}
 */
var findMedianSortedArrays = function (nums1, nums2) {
    var sum = nums1.length + nums2.length,
        start = 0,
        end = sum;

    while (start < end / 2) {

    }
};
